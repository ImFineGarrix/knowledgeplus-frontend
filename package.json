{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cp .env.production .env && NODE_ENV=prd nuxt build",
    "build:stg": "cp .env.dev .env && NODE_ENV=dev nuxt build",
    "dev": "cp .env.production .env && NODE_ENV=prd nuxt dev",
    "dev:stg": "cp .env.dev .env && NODE_ENV=dev nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start": "cp .env.production .env && NODE_ENV=prd nuxt start",
    "start:stg": "cp .env.dev .env && NODE_ENV=dev nuxt start"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "autoprefixer": "^10.4.16",
    "nuxt": "^3.8.0",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.3.5",
    "vue": "^3.3.7",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.5.1",
    "axios": "^1.6.0",
    "humps": "^2.0.1",
    "js-cookie": "^3.0.5",
    "jwt-decode": "^4.0.0",
    "pinia": "^2.1.7"
  }
}